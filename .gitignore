# Brainstorming visual-companion scratch (persisted mockups, not source)
.superpowers/

# ESP-IDF build output
build/
core/build/
firmware/build/
firmware/managed_components/
firmware/sdkconfig
sdkconfig.old

# IDE / clangd index caches
.cache/
**/.cache/

# Local credentials (never commit)
firmware/main/secrets.h
*.bin
*.elf
*.map

# Editor / OS
.DS_Store
.vscode/
.idea/
